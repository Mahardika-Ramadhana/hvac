#ifndef ROBOT_ESR_V2_JALAN_DITEMPAT_HPP
#define ROBOT_ESR_V2_JALAN_DITEMPAT_HPP

#include "RobotContext.hpp"

// Walk in place – root translation is held at (freezeX, freezeY).
void nodeJalanDitempat(RobotContext& robot,
                       double t,
                       double freezeX,
                       double freezeY);

#endif
