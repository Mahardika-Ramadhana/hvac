#pragma once

#include "RobotContext.hpp"

double nodeLompat(RobotContext& robot,
                  double t,
                  double duration,
                  double freezeX,
                  double freezeY,
                      double startYaw);
